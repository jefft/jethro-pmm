<?php
// Functional tests use the devbox MariaDB (socket-only; see tests/functional/bin/functest_database_setup)
// to (re)load the database).  MYSQL_UNIX_PORT is set by the devbox shell that
// launches the test services (`devbox info mariadb`).
#$dbname="jethro_functest";
#$dbsocket = getenv('MYSQL_UNIX_PORT');
#define('DB_DSN', "mysql:dbname=$dbname;unix_socket=$dbsocket");
#define('DB_USERNAME', 'jethro_functest');
#define('DB_PASSWORD', 'jethro_functest');
#define('PREFILL_USERNAME', 'demo');
#define('PREFILL_PASSWORD', 'qfntt7eYuwHs123');

define('SMS_LOCAL_PREFIX', '0');
define('SMS_INTERNATIONAL_PREFIX', '61');

define('SMS_VERBOSE', true);

// Do NOT hardcode ENABLED_FEATURES here: it silently disables every other
// feature (Notes, Attendance, Documents, ...) for the whole test run, not
// just narrows SMS-testing scope. Let it come from the `setting` table
// (see tests/functional/sms/functest_database_setup) like a real instance would.

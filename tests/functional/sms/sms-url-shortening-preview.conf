<?php
require_once __DIR__.'/common.conf';
define('SMS_PROVIDER', 'cellcast');
define('SMS_CELLCAST_APIKEY', 'test-mock-key');
// Test scenario: sms-url-shortening-preview
// SMS_SHORTEN_URLS auto-wraps bare https:// URLs via %(shorten "...")%.
// Preview uses previewShorten() which is local/deterministic — no external call.
define('SMS_CELLCAST_URL', '/smsmockserver/tests/functional/sms/sms-url-shortening-preview');
define('SMS_SHORTEN_URLS', true);
// conf.php's production URLSHORTENER/URLSHORTENER_API_KEY defines are past
// the test-mode early-return, so they never run here — define them directly.
// previewShorten() is local/deterministic and ignores the API key; only a
// non-empty URLSHORTENER is required to pass the shortening-enabled gate.
define('URLSHORTENER', 'jethroau');
define('URLSHORTENER_API_KEY', 'test-mock-key');

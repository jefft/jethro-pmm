<?php
require_once __DIR__.'/common.conf';
define('SMS_PROVIDER', 'cellcast');
define('SMS_CELLCAST_APIKEY', 'test-mock-key');
// Test scenario: sms-bulk-interactive
// Same setup as sms-bulk, but its own mock profile: the mock records only the
// most recent send per profile, and this scenario's spec asserts on that
// recording while sms-bulk.spec.ts is sending in parallel.
define('SMS_CELLCAST_URL', '/smsmockserver/tests/functional/sms/sms-bulk-interactive');
define('SMS_SENDER_OPTIONS', 'Jethro');

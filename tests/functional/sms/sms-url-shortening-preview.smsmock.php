<?php
/** sms-url-shortening-preview profile: default Cellcast behaviour (no overrides). */
use SmsMockServer\Provider\CellcastProfile;
CellcastProfile::register('sms-url-shortening-preview', function (CellcastProfile $p) { });
